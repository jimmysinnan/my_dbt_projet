version: 2

models:
  - name: int_local_bike_order
    description: '{{ doc("int_local_bike_order") }}'
    columns:
      - name: order_id
        description: "Primary key, unique identifier for each order."
        tests:
          - unique
          - not_null
